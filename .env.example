# Set these in Netlify → Site configuration → Environment variables.
# NEVER commit real values. The PIT lives server-side only (Netlify Functions).

GHL_PIT=          # sub-account Private Integration Token (read: contacts, opportunities, custom fields/locations)
GHL_PIPELINE_ID=  # Insurance Workflow pipeline id: qT9EmKMANkGoTm8IAuQ4
